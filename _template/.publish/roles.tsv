# roles.tsv — publish roles and the privilege tiers each may see (comma-separated). TAB-separated.
# `scripts/publish <role>` stages the wiki pages whose `privilege` is in the role's list,
# de-links anything pointing outside that set, and (if Quartz is set up) builds a site.
# Tiers: default | business-sensitive | personal-sensitive
# Add or edit rows to make a view for any role.
client	default
team	default,business-sensitive
owner	default,business-sensitive,personal-sensitive
